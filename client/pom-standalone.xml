<?xml version="1.0" encoding="UTF-8"?>
<project>
	<parent>
		<groupId>org.sakaiproject</groupId>
		<artifactId>gradebook-gwt</artifactId>
		<version>SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<artifactId>gradebook-gwt-client</artifactId>
	<packaging>war</packaging>
	<name>gradebook-gwt-client</name>
	<version>SNAPSHOT</version>
	
	<build>
		<directory>target</directory>
    	<outputDirectory>target/classes</outputDirectory>
    	<testOutputDirectory>target/test-classes</testOutputDirectory>
    	<sourceDirectory>src/java</sourceDirectory>
    	<scriptSourceDirectory>src/main/webapp</scriptSourceDirectory>
    	<testSourceDirectory>src/test</testSourceDirectory>
		<resources>
			<resource>
	   			<directory>${basedir}/src/java</directory>
           		<!--excludes>
					<exclude>**/*.java</exclude>
           		</excludes-->
			</resource>
		</resources>
		<plugins>
			<plugin>
        		<inherited>true</inherited>
        		<groupId>org.apache.maven.plugins</groupId>
        		<artifactId>maven-compiler-plugin</artifactId>
        		<configuration>
          			<source>1.5</source>
          			<target>1.5</target>
        		</configuration>
      		</plugin>
			<!-- All the fantastical settings that GWT needs -->
			<plugin>
				<groupId>com.totsp.gwt</groupId>
				<artifactId>maven-googlewebtoolkit2-plugin</artifactId>
				<version>2.0-beta20</version>
				<configuration>
					<gwtVersion>${gwt.version}-rc1</gwtVersion>
					<logLevel>INFO</logLevel>
					<compileTargets>
						<value>
							org.sakaiproject.gradebook.gwt.GradebookApplication
						</value>
					</compileTargets>
					<runTarget>
						org.sakaiproject.gradebook.gwt.GradebookApplication/GradebookApplication.html
					</runTarget>
					<generatorRootClasses>
						<value>
							org.sakaiproject.gradebook.gwt.GradebookApplication
						</value>
					</generatorRootClasses>
					<overwriteGeneratedClasses>
						true
					</overwriteGeneratedClasses>
					<!-- use style OBF for prod -->
					<style>OBF</style>
					<generateGettersAndSetters>
						true
					</generateGettersAndSetters>
					<noServer>false</noServer>
					<extraJvmArgs>-Dgwt.coverage.enable=true</extraJvmArgs>
				</configuration>
				<executions>
					<execution>
						<goals>
							<!-- autoconfig gwt (download and unzip GWT itself) -->
							<!-- *note* you can also use manual config, if you already have GWT installed -->
							<!-- see site docs for details - http://gwt-maven.googlecode.com/svn/docs/maven-googlewebtoolkit2-plugin/setup.html -->
							<goal>setup</goal>
							<goal>extractGwt</goal>

							<!-- actual work goals -->
							<goal>mergewebxml</goal>
							<goal>compile</goal>
							<goal>gwt</goal>
							<!--  uncomment testGwt to see the testing and code coverage magic -->
							<!-- <goal>testGwt</goal> -->
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<configuration>
                    <webXml>target/web.xml</webXml>
                </configuration>
			</plugin>
			<plugin>
        		<inherited>true</inherited>
        		<groupId>org.sakaiproject.maven.plugins</groupId>
        		<artifactId>sakai</artifactId>
        		<version>1.0</version>
        		<extensions>true</extensions>
        		<configuration>
          			<deployDirectory>${maven.tomcat.home}</deployDirectory>
          			<warSourceDirectory>${basedir}/src/main/webapp</warSourceDirectory>
        		</configuration>
      		</plugin>
      		<plugin>
        		<inherited>true</inherited>
        		<groupId>org.apache.maven.plugins</groupId>
        		<artifactId>maven-war-plugin</artifactId>
        		<version>2.0</version>
        		<configuration>
          			<warSourceDirectory>${basedir}/src/main/webapp</warSourceDirectory>
        		</configuration>
      		</plugin>
      		<plugin>
        	<inherited>true</inherited>
        		<groupId>org.apache.maven.plugins</groupId>
        		<artifactId>maven-release-plugin</artifactId>
        		<configuration>
                	<tagbase>https://source.sakaiproject.org/svn-test/sakai/tags</tagbase>
            		<preparationGoals>clean verify install</preparationGoals>
            		<autoVersionSubmodules>true</autoVersionSubmodules>
            		<useReleaseProfile>true</useReleaseProfile>
        		</configuration>
       		</plugin>
			
			<plugin>
				<!--  use AntRun to do the Emma report (GWT emits coverage data files on it's own) -->				 
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<!-- 
					<execution>
						<id>emma-coverage-report</id>
						<phase>pre-site</phase>
						<configuration>
							<tasks>
								<taskdef resource="emma_ant.properties"
									classpathref="maven.plugin.classpath" />
								<emma enabled="true">
									<report>
										<infileset
											dir="${project.build.directory}" includes="*.em, *.ec" />
										<html
											outfile="${project.build.directory}/coverage/coverage.html"
											depth="method" columns="name,class,method,block,line" />
									</report>
								</emma>
								<copy
									todir="${project.build.directory}/site/">
									<fileset
										dir="${project.build.directory}/coverage" />
								</copy>
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
					-->
				</executions>
				<dependencies>
					<!--dependency>
						<groupId>emma</groupId>
						<artifactId>emma</artifactId>
						<version>2.0.5312</version>
					</dependency>
					<dependency>
						<groupId>emma</groupId>
						<artifactId>emma_ant</artifactId>
						<version>2.0.5312</version>
					</dependency-->
				</dependencies>
			</plugin>		
		</plugins>
		<!--finalName>gradebook-gwt-client</finalName-->
	</build>
	
	<!--  standard Maven reporting done here -->
	<reporting>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>
					maven-project-info-reports-plugin
				</artifactId>
				<reportSets>
					<reportSet>
						<reports>
							<report>dependencies</report>
						</reports>
					</reportSet>
				</reportSets>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-report-plugin</artifactId>
				<!--  the Maven Surefire Report plugin has a bug where it re-runs the tests, 
				this is needed to avoid that  - http://jira.codehaus.org/browse/SUREFIRE-257 -->
				<reportSets>
					<reportSet>
						<reports>
							<report>report-only</report>
						</reports>
					</reportSet>
				</reportSets>				
			</plugin>
		</plugins>
	</reporting>
	
	<dependencies>	
		
		<!-- GWT DEPENDENCY -->
		<dependency>
			<groupId>com.google.gwt</groupId>
			<artifactId>gwt-servlet</artifactId>
			<version>${gwt.version}</version>
			<scope>compile</scope>
		</dependency>
		
		<!-- GWT INCUBATOR DEPENDENCY -->
		<dependency>
			<groupId>com.google.gwt</groupId>
			<artifactId>gwt-incubator</artifactId>
			<version>JLR</version>
		</dependency>
		
		<dependency>
                       <groupId>com.allen-sauer</groupId>
                       <artifactId>gwt-dnd</artifactId>
                       <version>2.5.3</version>
        </dependency>

		<dependency>
			<groupId>commons-lang</groupId>
			<artifactId>commons-lang</artifactId>
			<version>2.3</version>
		</dependency>

		<!-- SAKAI DEPENDENCIES -->
		<!--dependency>
			<groupId>org.sakaiproject</groupId>
			<artifactId>sakai-gradebook-service-api</artifactId>
			<version>${sakai.version}</version>
		</dependency-->
		
	
		<!--  coverage patch (used to generate code coverage data for GwtTestCase based tests, when -Dgwt.coverage.enable=true -->
		<!--dependency>
			<groupId>com.google.gwt</groupId>
			<artifactId>gwtcoverage</artifactId>
			<version>1.5.0</version>
			<scope>provided</scope>
		</dependency-->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.1</version>
			<scope>test</scope>
		</dependency>
		<!--dependency>
			<groupId>emma</groupId>
			<artifactId>emma</artifactId>
			<version>2.0.5312</version>
			<scope>test</scope>
		</dependency-->
	</dependencies>	
</project>

